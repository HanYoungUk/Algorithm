using System;

public class Solution {
    public string solution(string n_str) {
        string answer = "";
        int x = Int32.Parse(n_str);
        answer = x.ToString();
       
        return answer;
    }
}